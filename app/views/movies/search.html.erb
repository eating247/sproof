<div class="col-sm-offset-2 col-sm-8">
  <h1 class="text-center"><%= pluralize( @movies.length, 'Search Result') %></h1>

  <% @movies.each do |movie| %>
    <%= render partial: "movie", locals: { movie: movie } %>
  <% end %>
</div>
